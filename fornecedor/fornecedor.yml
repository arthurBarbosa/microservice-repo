spring:
    jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/fornecedor
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
